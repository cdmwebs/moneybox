<h3>Envelopes</h3>
<ul>
<% @envelopes.each do |envelope| %>
  <%= content_tag :li do %>
    <%= envelope.name %>
    <%= content_tag :span, envelope.balance.format, class: "badge #{envelope.badge_style}" %>
  <% end %>
<% end %>
</ul>