<%= simple_form_for @envelope, do |f| %>
  <%= f.input :name %>
  <%= f.input :income %>
  <%= f.input :balance, as: :numeric, input_html: {step: "0.01"} %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>